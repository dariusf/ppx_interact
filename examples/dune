(executables
 (names example simple ret)
 ; the containers dependency isn't strictly needed,
 ; it's just to show that third-party libraries can be used
 (libraries ppx_interact_runtime containers)
 (modes byte)
 (link_flags -linkall)
 (preprocess
  (pps ppx_interact)))
